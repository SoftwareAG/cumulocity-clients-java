package com.cumulocity.model.location.sensor;

import com.cumulocity.model.util.Alias;


/**
 * Provides a representation for a GPS sensor.
 * 
 * A GPS sensor provides location information, consisting of latitude, longitude, and altitude, as retrieved from the GPS network of satellites..
 * 
 * At this moment in time, this representation does not have any properties of its own.
 * @author ricardomarques
 *
 */
@Alias(value = "c8y_GPSSensor")
public class GPSSensor  {
    // So far, this representation does not have any properties of its own.
}
