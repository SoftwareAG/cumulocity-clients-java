package c8y;

import org.svenson.AbstractDynamicProperties;

/**
 * Provides a representation for a temperature sensor.
 *
 * A temperature sensor, or thermometer, reports degrees of hotness.
 *
 * At this moment in time, this representation does not have any properties of its own.
 * @author ricardomarques
 *
 */
public class TemperatureSensor extends AbstractDynamicProperties {
    // So far, this representation does not have any properties of its own.
}
